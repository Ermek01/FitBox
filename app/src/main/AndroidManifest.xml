<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="kg.smartpost.fitbox">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitBox">
        <activity android:name=".ui.activities.OrderProgramActivity"></activity>
        <activity android:name=".ui.activities.ExampleDietsActivity" />
        <activity android:name=".ui.activities.LoseOrGainWeightActivity" />
        <activity android:name=".ui.activities.FreeProgramsActivity" />
        <activity android:name=".ui.activities.OrderingActivity" />
        <activity android:name=".ui.activities.MarathonActivity" />
        <activity android:name=".ui.activities.WorkoutActivity" />
        <activity android:name=".ui.activities.NewsActivity" />
        <activity android:name=".ui.activities.ShopActivity" />
        <activity android:name=".ui.activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.activities.MainActivity" />
    </application>

</manifest>